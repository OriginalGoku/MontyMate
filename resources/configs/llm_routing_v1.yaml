llm_routing:
  version: 1

# Role -> model/provider selection (multi-LLM by design)
roles:
  interviewer:
    primary: { provider: openai, model: gpt-4.1-mini }
    fallback:
      - { provider: anthropic, model: claude-3-5-sonnet }
  researcher:
    primary: { provider: anthropic, model: claude-3-5-sonnet }
    fallback:
      - { provider: openai, model: gpt-4.1 }
  architect:
    primary: { provider: openai, model: gpt-4.1 }
    fallback:
      - { provider: anthropic, model: claude-3-5-sonnet }
  reviewer:
    primary: { provider: openai, model: gpt-4.1 }
    fallback:
      - { provider: anthropic, model: claude-3-5-sonnet }
  builder:
    primary: { provider: openai, model: gpt-4.1 }
    fallback:
      - { provider: anthropic, model: claude-3-5-sonnet }
  qa:
    primary: { provider: openai, model: gpt-4.1-mini }
    fallback:
      - { provider: openai, model: gpt-4.1 }

# Pricing lookup keys used by your SQLite pricing tables (optional)
pricing_keys:
  unit: per_1k_tokens
  currency: USD
